Place embedded MongoDB binary here for offline packaging:

Required file:
- mongod.exe

You can fetch it automatically from project root with:
- npm run desktop:fetch-mongo

This directory is packaged into the final EXE resources at:
- mongodb/bin/mongod.exe
